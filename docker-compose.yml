version: '3.8'

services:
  browser:
    build: .
    
    volumes:
      - ./config.yml:/app/config.yml  # Mount config file for easy updates
    environment:
      - NO_SANDBOX=1  # Required for running Chrome in Docker
      - DISPLAY=:99  # For Xvfb
    shm_size: '2gb'  # Shared memory size for browser stability